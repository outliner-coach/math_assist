# 4학년 Bridge 단원별 출시 범위

## 현재 상태

4학년은 교육과정 원장에서 `released`이며 홈의 1·2·3·4·5·6학년 선택에
노출된다. `/grade/4`는 검증된 단원을 선택하고,
`/grade/4/mission?unitId=<id>`는 선택 단원의 3문제 Bridge 활동을
제공한다. 6학년 공개 범위는 별도의 Study 계약을 따른다.

## 검증된 범위

- 단원 1: `unit-4-1-large-numbers` 큰 수
  - 성취기준: `[4수01-01]`, `[4수01-02]`
  - 콘텐츠 버전: `grade4-bridge-big-numbers-v1`
- 단원 2: `unit-4-1-multiplication-division` 두 자리 수로 나누기
  - 성취기준: `[4수01-07]`
  - 콘텐츠 버전: `grade4-bridge-two-digit-division-v1`
- 단원 3: `unit-4-1-arithmetic-estimation` 사칙계산 어림
  - 성취기준: `[4수01-08]`
  - 콘텐츠 버전: `grade4-bridge-arithmetic-estimation-v1`
- 단원 4: `unit-4-2-decimals` 소수
  - 성취기준: `[4수01-13]`, `[4수01-14]`
  - 콘텐츠 버전: `grade4-bridge-decimals-v1`
- 단원 5: `unit-4-2-fraction-add-sub` 분수의 덧셈과 뺄셈
  - 성취기준: `[4수01-15]`
  - 콘텐츠 버전: `grade4-bridge-fraction-add-sub-v1`
- 단원 6: `unit-4-2-decimal-add-sub` 소수의 덧셈과 뺄셈
  - 성취기준: `[4수01-16]`
  - 콘텐츠 버전: `grade4-bridge-decimal-add-sub-v1`
- 단원 7: `unit-4-2-patterns` 규칙 찾기
  - 성취기준: `[4수02-01]`, `[4수02-02]`
  - 콘텐츠 버전: `grade4-bridge-patterns-v1`
- 단원 8: `unit-4-2-equality` 등호와 양의 관계
  - 성취기준: `[4수02-03]`
  - 콘텐츠 버전: `grade4-bridge-equality-v1`
- 단원 9: `unit-4-1-perpendicular-parallel` 수직과 평행
  - 성취기준: `[4수03-03]`
  - 콘텐츠 버전: `grade4-bridge-perpendicular-parallel-v1`
- 단원 10: `unit-4-1-shape-transformations` 도형의 이동
  - 성취기준: `[4수03-04]`, `[4수03-05]`
  - 콘텐츠 버전: `grade4-bridge-shape-transformations-v1`
- 단원 11: `unit-4-2-triangles` 여러 가지 삼각형
  - 성취기준: `[4수03-08]`, `[4수03-09]`
  - 콘텐츠 버전: `grade4-bridge-triangles-v1`
- 단원 12: `unit-4-2-quadrilaterals` 여러 가지 사각형
  - 성취기준: `[4수03-10]`
  - 콘텐츠 버전: `grade4-bridge-quadrilaterals-v1`
- 단원 13: `unit-4-2-polygons` 다각형과 모양 채우기
  - 성취기준: `[4수03-11]`, `[4수03-12]`
  - 콘텐츠 버전: `grade4-bridge-polygons-v1`
- 단원 14: `unit-4-1-angle-measurement` 각도와 내각의 합
  - 성취기준: `[4수03-24]`, `[4수03-25]`
  - 콘텐츠 버전: `grade4-bridge-angle-measurement-v1`
- 각 단원 문제 틀: 10개, 인지영역 K/A/R `4/4/2`
- 한 활동: 알기·적용·추론 각 1개, 총 3문제
- 표현: 자리값 표, 수 카드, 수직선, 생활 맥락, 답을 가린 세로
  나눗셈 모형, 피연산자에서 파생한 정량 분수 띠와 소수 세로셈,
  제시값에서 빈칸을 파생하는 수열·대응·계산식 배열표, 같은 두 양을
  나란히 보여 주는 등식 균형 모형, 방향각에서 직선·직각 표식을
  파생하는 수직·평행 모형, 시작점·이동량·기준선·중심에서 목표
  도형을 파생하는 좌표 격자 이동 모형, 세 변에서 좌표와 직각 표식을
  파생하는 정량 삼각형 모형, 꼭짓점에서 평행·직각·같은 변 표식을
  함께 파생하는 사각형 성질 모형, 변 수에서 정다각형·대각선을
  파생하는 다각형 모형, 행·열·타일 위상에서 조각과 빈틈을 파생하는
  모양 채우기 모형, 각도 값에서 눈금·광선·회전 방향을 파생하는
  각도기 모형, 주어진 내각에서 꼭짓점과 미지각을 파생하는
  삼각형·평행사변형 내각 합 모형
- 채점: 선택형 완전 일치, 정수형 완전한 부호 있는 정수, 소수형 완전한
  십진 표기만 허용하며 소수 끝의 0은 값 동치로 정규화, 분수형은
  완전한 분수·대분수만 허용하며 동치인 가분수·대분수를 같은 값으로 비교
- 저장: `mathAssist_grade4Progress`와 공통 `mathAssist_attemptReceipts_v1`; 다른 학년 키와 분리
- 풀이장: 활동 실행·문항 변형별 안정적인 로컬 저장 키 사용

## 비활성 범위

원장에서 4학년으로 배정된 나머지 1개 성취기준은 `planned`를 유지한다.
문제 은행, 정답·그림 쌍 검사, 모바일 E2E가 모두 없는 단원은 화면에
만들거나 홈에서 가능하다고 표시하지 않는다.

## 공개 게이트

1. `npm run validate:curriculum`, `npm run validate:grade4`, `npm run audit:missions`가 오류와 경고 없이 끝난다.
2. 집중 단위 테스트와 전체 테스트, lint, TDD guard, production build가 통과한다.
3. 4학년 E2E에서 형식 오류 미기록, 오답→정답 영수증 순번, 로컬 진도, 3문제 완료, 작은 화면 폭을 확인한다.
4. 원장의 `releaseState.grade4 = released`와 홈의 지원 학년 4 노출은 같은 통합 변경으로 유지한다. 둘이 다르면 curriculum validator가 실패한다.

## 유지보수 계약

- 추가 문제는 반드시 원장에 4학년으로 배정되고 같은 변경에서
  `released`·`existing-reference`로 승격한 성취기준을 사용한다.
- 검증기는 각 단원을 독립적으로 10문제·K4/A4/R2·추론 문제군
  2개·표현 2개 이상인지 확인한다. 전역 합계만 맞추면 통과할 수 없다.
- 선수 기준 코드는 학습 순서 근거이며 해당 단원 문제의 출시 참조로
  세지 않는다.
- 정답 전용 합성값은 제출 전 DOM 또는 접근성 트리에 만들지 않는다.
- 소수 자리표는 문제에 주어진 자리 숫자는 보이되 합성된 소수 답을
  제출 전 하나의 DOM 문자열로 만들지 않는다.
- 분수 띠의 등분 수와 색칠 칸은 문제의 분모·분자에서 직접 만들고,
  합·차나 빠진 분자는 제출 전 DOM·접근성 이름에 만들지 않는다.
- 소수 덧셈·뺄셈은 피연산자와 합·차·빠진 항을 백분의 일 단위
  정수에서 계산한다. 세로셈은 피연산자 자리만 표시하고 결과는 제출
  전 DOM·접근성 이름에 만들지 않는다.
- 규칙표는 제시된 수열·대응쌍·계산식만 표시한다. 빈칸 값과 먼 단계
  결과는 제출 전 시각 설정·DOM·접근성 이름에 만들지 않는다.
- 사각형은 도형 종류와 치수에서 꼭짓점을 만들고, 같은 네 꼭짓점에서
  평행·직각·같은 변 표식을 계산한다. 정사각형은 직사각형과 마름모의
  성질도 가지는 포함 관계를 유지한다.
- 다각형은 변 수에서 꼭짓점과 한 꼭짓점의 `n-3`개 대각선을 만든다.
  정다각형은 모든 변과 모든 각의 같음 표시를 함께 가지며, 같은 변만
  표시된 반례를 정다각형으로 바꾸지 않는다.
- 모양 채우기는 행과 열에서 타일 수를 계산하고 실제 타일 노드도 같은
  수로 만든다. 정오각형 세 개는 한 꼭짓점에서 변을 공유하며 내각
  세 개 뒤의 36° 빈틈을 실제 좌표로 남긴다.
- 각도기 모형은 문제·정답·풀이와 같은 각도 값에서 눈금과 광선
  끝점을 계산한다. 미지 내각은 `□`로만 표시하고, 제출 전 정답 전용
  라벨이나 접근성 텍스트를 만들지 않는다. 평행사변형처럼 알려진
  다른 꼭짓점의 각이 미지각과 우연히 같을 수 있으므로, 선노출 검사는
  값 전체의 부재가 아니라 숨긴 꼭짓점 노드의 내용과 출처를 검사한다.
- 등식 균형 모형은 문제에서 제시한 양과 빈칸만 표시한다. 빠진 양은
  제출 전 시각 설정·DOM·접근성 이름에 만들지 않는다.
- 수직·평행 모형은 문제·정답·풀이와 같은 두 방향각에서 선의 방향과
  직각 표식을 만든다. 수직은 방향 차 90°, 평행은 방향 차 0°라는
  조건을 모든 변형에서 검사한다.
- 도형 이동 모형은 시작 위치, 이동량, 기준선, 회전 중심만 시각
  설정에 저장한다. 좌표 답과 목표 도형은 같은 변환 함수에서 계산하되
  확인 전 DOM·접근성 이름에 만들지 않는다.
- 삼각형 모형은 세 변과 삼각형 부등식에서 꼭짓점 좌표를 계산한다.
  변 분류와 각 분류, 직각 표식은 같은 세 변 모델에서 파생한다.
- 조건형 문제는 모든 숫자 변형을 직접 계산한 참값과 대조한다.
- 어림값은 화면에 표시된 수와 단위에서 파생하며, 방법 비교는 모든
  변형에서 오차가 더 작은 한 방법만 정답이 되도록 검사한다.
- 유효한 확인만 시도 영수증을 만들며 원답 문자열과 풀이장 획은 영수증에 저장하지 않는다.

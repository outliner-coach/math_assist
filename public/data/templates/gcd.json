[
  {
    "id": "tmpl-gcd-choice-001",
    "concept_id": "gcd-001",
    "type": "choice",
    "difficulty": 1,
    "param_schema": {
      "a": { "min": 6, "max": 15 },
      "b": { "min": 8, "max": 20 }
    },
    "prompt_template": "{{a}}와 {{b}}의 최대공약수는?",
    "choices_template": [
      "{{gcd(a, b)}}",
      "{{gcd(a, b) + 2}}",
      "{{gcd(a, b) + 5}}",
      "{{gcd(a, b) + 9}}"
    ],
    "solver_rule": "gcd(a, b)",
    "solution_steps_template": [
      "{{a}}의 약수: {{divisors(a)}}",
      "{{b}}의 약수: {{divisors(b)}}",
      "공약수: {{commonDivisors(a, b)}}",
      "최대공약수: {{gcd(a, b)}}"
    ]
  },
  {
    "id": "tmpl-gcd-number-001",
    "concept_id": "gcd-001",
    "type": "number",
    "difficulty": 2,
    "param_schema": {
      "a": { "min": 8, "max": 18 },
      "b": { "min": 12, "max": 24 }
    },
    "prompt_template": "{{a}}와 {{b}}의 최대공약수를 구하세요.",
    "solver_rule": "gcd(a, b)",
    "solution_steps_template": [
      "{{a}}의 약수: {{divisors(a)}}",
      "{{b}}의 약수: {{divisors(b)}}",
      "공약수: {{commonDivisors(a, b)}}",
      "최대공약수: {{gcd(a, b)}}"
    ]
  },
  {
    "id": "tmpl-gcd-choice-002",
    "concept_id": "gcd-001",
    "type": "choice",
    "difficulty": 2,
    "param_schema": {
      "a": { "min": 10, "max": 20 },
      "b": { "min": 15, "max": 30 }
    },
    "prompt_template": "다음 중 {{a}}와 {{b}}의 공약수인 것은?",
    "choices_template": [
      "1",
      "{{a + b}}",
      "{{a + b + 3}}",
      "{{a + b + 7}}"
    ],
    "solver_rule": "1",
    "solution_steps_template": [
      "{{a}}의 약수: {{divisors(a)}}",
      "{{b}}의 약수: {{divisors(b)}}",
      "공약수: {{commonDivisors(a, b)}}",
      "1은 모든 수의 공약수입니다."
    ]
  }
]

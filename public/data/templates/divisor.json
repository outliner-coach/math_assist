[
  {
    "id": "tmpl-divisor-choice-001",
    "concept_id": "divisor-001",
    "type": "choice",
    "difficulty": 1,
    "param_schema": {
      "n": { "min": 6, "max": 20 }
    },
    "prompt_template": "다음 중 {{n}}의 약수인 것은?",
    "choices_template": [
      "{{n}}",
      "{{n + 1}}",
      "{{n + 2}}",
      "{{n + 3}}"
    ],
    "solver_rule": "n",
    "solution_steps_template": [
      "{{n}}의 약수를 구합니다: {{divisors(n)}}",
      "{{n}}은 자기 자신의 약수입니다.",
      "{{n + 1}}, {{n + 2}}, {{n + 3}}은 {{n}}보다 크므로 약수가 아닙니다."
    ]
  },
  {
    "id": "tmpl-divisor-number-001",
    "concept_id": "divisor-001",
    "type": "number",
    "difficulty": 1,
    "param_schema": {
      "n": { "min": 10, "max": 30 }
    },
    "prompt_template": "{{n}}의 약수 중 가장 큰 수는?",
    "solver_rule": "n",
    "solution_steps_template": [
      "{{n}}의 약수를 구합니다: {{divisors(n)}}",
      "가장 큰 약수는 자기 자신인 {{n}}입니다."
    ]
  },
  {
    "id": "tmpl-divisor-choice-002",
    "concept_id": "divisor-001",
    "type": "choice",
    "difficulty": 2,
    "param_schema": {
      "n": { "min": 12, "max": 36 }
    },
    "prompt_template": "{{n}}의 약수는 모두 몇 개인가요?",
    "choices_template": [
      "{{divisorCount(n)}}",
      "{{divisorCount(n) + 2}}",
      "{{divisorCount(n) + 4}}",
      "{{divisorCount(n) + 6}}"
    ],
    "solver_rule": "divisorCount(n)",
    "solution_steps_template": [
      "{{n}}의 약수를 모두 구합니다: {{divisors(n)}}",
      "약수의 개수를 세면 {{divisorCount(n)}}개입니다."
    ]
  }
]
